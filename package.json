{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build --concurrency 1",
    "deploy": "yarn publish --access public"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.4.1",
    "lerna": "^8.1.8",
    "nx": "^19.6.1",
    "typescript": "^5.5.4"
  }
}
